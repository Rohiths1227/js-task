console.log("SECTION 6 – TERNARY")

let age = 20
console.log(age >= 18 ? "Adult" : "Minor")

let marks = 40
console.log(marks >= 50 ? "Pass" : "Fail")

let num = 8
console.log(num % 2 == 0 ? "Even" : "Odd")

let grade = 85
console.log(grade >= 90 ? "A" : grade >= 75 ? "B" : "C")

let n1 = -5
console.log(n1 >= 0 ? "Positive" : "Negative")
